'use strict';
import React from 'react-native';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    goBackBtn: {
        color: "green",
        fontSize: 15,
        padding: 10,
        display: 'flex',
        borderBottomWidth: 1,
    },
});